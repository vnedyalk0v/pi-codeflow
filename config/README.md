# Config

Codeflow will eventually read project configuration from `.pi/codeflow.json` and merge it with `default.codeflow.json`.

`checks` is an ordered array. Leave it empty when a repository has no implementation yet. Add commands only after they exist in the target project.
