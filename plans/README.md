# Implementation Plans

Generated by the improve skill on 2026-06-18, against commit `554971d` (branch
`dev`). Each executor: read the plan fully before starting, honor its STOP
conditions, run the drift check first, and update your row when done.

These four plans are **independent** — there are no hard dependencies between
them, so they can be executed in any order or in parallel branches. If you want
to build executor confidence first, do the small low-risk ones (003, then 002)
before the larger 001; the design plan (004) ships no production code.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Enforce-or-document every `safety.*` config flag | P1 | M | MED | — | DONE |
| 002 | Validate base/head refs before they reach git/gh | P2 | S | LOW | — | TODO |
| 003 | Stop `getAheadCount` from returning NaN | P3 | S | LOW | — | TODO |
| 004 | Design `/flow-watch` (GitHub checks watcher) — design + spike | P2 (direction) | M | LOW | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED
(one-line rationale).

## Dependency notes

- None of 001–004 depend on each other. Plan 004 *produces* a follow-up plan
  `005-flow-watch-implementation.md`; plan 005 will depend on 004's ADR landing.

## What was audited (and what wasn't)

Audited directly (no subagents — repo is ~8.2K LOC, single-pass range): the
security-critical core — config loading/validation, the `/flow-*` command layer,
git/GitHub clients, the check runner, redaction, session/check/PR state,
branching/branch-name, guidance injection, and the `before_agent_start`
extension entry. Verification baseline confirmed green: `tsc --noEmit` clean,
263 tests pass across 37 files; `execFile` (no shell) used for git/gh; redaction
wired into display and stored state.

Not deeply audited: the prompt/template markdown files under `prompts/` and
`templates/` (content, not code), the custom check scripts in `scripts/`, the
full schema files beyond the `safety` block, and the `config/example.*` configs
beyond confirming the `safety` flag values. No `npm audit` advisory review was
performed (single runtime dep, `ajv`); worth a separate pass if desired.

## Findings considered and rejected

- **Regex redaction misses bare high-entropy tokens** (tokens without a known
  prefix or `key: value` shape pass through `redactSecrets`): by design.
  `docs/SECURITY_MODEL.md` frames redaction as best-effort token hygiene, not a
  guarantee. Not worth a plan.
- **Session state stores raw check stdout/stderr**: investigated and false —
  `src/state/check-state.ts:44-58` stores only redacted, truncated summaries
  (via `truncateForSummary`), never raw output. No finding.
- **`allowDestructiveGitOperations` / `allowForcePush` unread**: folded into plan
  001 as "reserved for future" documentation, not separate findings — the
  features they gate genuinely do not exist yet, so the flags are legitimately
  inert-for-now (the fix is honest docs, covered by 001).
- **No linter/formatter (F4)**: real absence, but a maintainer judgment call
  given `AGENTS.md`'s "no dependencies without a documented rationale" rule. Not
  turned into a plan this round; revisit if the maintainer wants ESLint/Biome.
